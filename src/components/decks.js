import React from 'react'
import Deck from './deck'
const Decks = (props) => {
    console.log(props)
    return (
        <div>
           
            <Deck/>
        </div>
    )
}

export default Decks
